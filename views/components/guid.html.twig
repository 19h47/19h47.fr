<div class="Guid pointer-events-none hidden">
	<div class="relative mx-auto h-full max-w-full px-32">
		<div class="grid h-full grid-cols-12 gap-x-16">

		{% for i in 1..12 %}
		    <div class="col-span-1 h-full text-center">
				<div class="Guid__column">
					{{- i -}}
				</div>
			</div>
		{% endfor %}

		</div>
	</div>
</div>
